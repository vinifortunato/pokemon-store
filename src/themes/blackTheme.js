import defaultTheme from './defaultTheme';

const theme = {
  breakpoints: {
    ...defaultTheme.breakpoints,
  },
  colors: {
    ...defaultTheme.colors,
    background: '#6F6E78',
    primary: '#58575F',
  },
};

export default theme;
