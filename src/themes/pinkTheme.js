import defaultTheme from './defaultTheme';

const theme = {
  breakpoints: {
    ...defaultTheme.breakpoints,
  },
  colors: {
    ...defaultTheme.colors,
    background: '#EBA8C3',
    primary: '#ED6EC7',
  },
};

export default theme;
