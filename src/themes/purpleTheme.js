import defaultTheme from './defaultTheme';

const theme = {
  breakpoints: {
    ...defaultTheme.breakpoints,
  },
  colors: {
    ...defaultTheme.colors,
    background: '#9F6E97',
    primary: '#A552CC',
  },
};

export default theme;
