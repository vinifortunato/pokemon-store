import defaultTheme from './defaultTheme';

const theme = {
  breakpoints: {
    ...defaultTheme.breakpoints,
  },
  colors: {
    ...defaultTheme.colors,
    background: '#FF6568',
    primary: '#EA5D60',
  },
};

export default theme;
