import defaultTheme from './defaultTheme';

const theme = {
  breakpoints: {
    ...defaultTheme.breakpoints,
  },
  colors: {
    ...defaultTheme.colors,
    background: '#F2CB55',
    primary: '#EED535',
  },
};

export default theme;
